default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: active_record_search_and_sort_development

test:
  <<: *default
  database: active_record_search_and_sort_test
